---
import links from '@/public/links.json'

import PageLayout from '@/layouts/CommonPage.astro'
import FriendList from '@/components/links/FriendList.astro'

const headings = [
  { depth: 2, slug: 'friends', text: '朋友们' },
  { depth: 2, slug: 'special', text: '常用入口' },
  { depth: 2, slug: 'exchange', text: '交换友链' }
]

const { friends } = links
---

<PageLayout title='友链' {headings}>
  <p>这里收藏朋友们在互联网上认真经营的独立空间。</p>

  <h2 id='friends'>朋友们</h2>
  {
    friends[0].link_list.length > 0 ? (
      <FriendList list={friends[0]} />
    ) : (
      <p class='text-muted-foreground'>友链正在慢慢收集中，欢迎成为这里的第一位朋友。</p>
    )
  }

  <h2 id='special'>常用入口</h2>
  <FriendList list={friends[2]} />

  <h2 id='exchange'>交换友链</h2>
  <p>如果你也在维护一个独立网站，可以通过 GitHub 联系我交换友链。请附上：</p>
  <ul>
    <li>网站名称与一句话介绍</li>
    <li>网站地址与头像地址</li>
    <li>保持站点可以正常访问并持续更新</li>
  </ul>
  <p><a href='https://github.com/wxa1225' target='_blank'>在 GitHub 上联系 Sia →</a></p>
</PageLayout>
